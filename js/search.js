const searchInput   = document.getElementById('search');
const searchBox     = document.querySelector('.search__results');
const searchResults = document.querySelectorAll('.search__result');

searchInput.addEventListener('keyup', (e)=>{
    e.preventDefault();


})